# Editor Contribution Guide

## VS Code Extension

From `editors/vscode/`:

```bash
pnpm install
pnpm fmt
pnpm lint
pnpm check-types
pnpm test
```

## Neovim Plugin

From `editors/neovim/`:

```bash
./test.sh
```

Requires `nvim` in PATH.
